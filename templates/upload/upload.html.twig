{% extends 'base.html.twig' %}

{% block stylesheets %}
	{{parent()}}
{% endblock %}
{% block body %}
{{parent()}}
<div class="mt-2">
		<h1 class="d-flex justify-content-center mt-5 pt-5">Poster</h1>
		{{form_start(form)}}
		{{form_rest(form)}}
        <button type="submit" class="btn btn-primary">Envoyer la photo!</button>
		{{form_end(form)}}
	</div>
{% endblock %}
